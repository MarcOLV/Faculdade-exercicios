algoritmo exemplo4
	inteiro altura, base
	real area, per
	inicio
	escreva("digite a altura do retangulo:")
	leia(altura)
	escreva("digite a base do retangulo:")
	leia(base)
	per = ((base*2) + (altura*2))
	area = base * altura
	escreva("perimetro: " + per + " area : " + area)
	fim
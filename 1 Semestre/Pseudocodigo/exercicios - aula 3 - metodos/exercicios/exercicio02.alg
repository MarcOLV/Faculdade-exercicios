algoritmo exercicio2
	real lado1, lado2, area
	
	inicio
	escreva("Digite o primeiro valor do retangulo: ")
	leia(lado1)
	escreva("Digite o primeiro valor do retangulo: ")
	leia(lado2)
	escreva("O valor da area é: " + area(lado1, lado2) )



	fim
	
	real area(real lado1, real lado2)
	inicio

		retorne lado1 * lado2
	
	fim